set -e

pnpm exec biome check --staged --no-errors-on-unmatched
# Testes unitários dos projetos afetados: TS (vitest) + Python (rag-service via
# container/.venv, com skip gracioso — ver apps/rag-service/scripts/run-tests.sh).
pnpm exec nx affected -t vitest:test test
